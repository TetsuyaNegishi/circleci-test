version: 2.1

jobs:
  test:
    docker:
      - image: circleci/node:12.14.0
    working_directory: ~/work
    steps:
      - checkout
      - run: yarn i
      - run:
          name: type check
          command: yarn type-check
